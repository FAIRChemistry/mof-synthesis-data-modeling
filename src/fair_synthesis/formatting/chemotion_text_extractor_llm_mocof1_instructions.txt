You are given Chemotion free text fields for one experiment:
- plain_text_description: reaction setup and procedure text
- plain_text_observation: workup and analysis text

Return a JSON object conforming exactly to the provided schema.
Use only information explicitly present in the text. Do not infer values from chemistry knowledge.
If a field is not applicable, use null for nullable fields, false for booleans, and [] for rinse.

Searches are case-insensitive.

For vessel, analyze plain_text_description:
- If the text contains "microwave vial", use "microwave vial".
- Else if the text contains "pressure tube", "J. Young tube", or "Schlenk bomb", use "Schlenk bomb".
- Else use null.

For degassing, analyze plain_text_description:
- If the text contains "FPT" or "Ar replace", use "Ar".
- Else use null.

For rinse, analyze plain_text_observation:
- Search for these supported solvents only: acetone, Et3N, MeCN, NaCl aq, DMF, CHCl3, MeOH, EtOH, dioxane/nitrobenzene.
- Treat "ethanol" as EtOH.
- Treat "dioxane/PhNO2", "dioxane/PhNO₂", "PhNO2/dioxane", and "PhNO₂/dioxane" as dioxane/nitrobenzene.
- Include each supported solvent found in the text once, using the exact schema spelling.

For wait_after_rinse, analyze plain_text_observation:
- If the text contains "Soxhlet" or "open to air", set wait_after_rinse to 24 and wait_after_rinse_unit to "h".
- Else set wait_after_rinse to null and wait_after_rinse_unit to "h".

For wash_solid, analyze plain_text_observation:
- If the text contains "samples under fillers" or "MeOH filled up", use "MeOH+scCO2".
- Else if the text contains "supercritical CO2", "scCO2", or "scCO₂", use "scCO2".
- Else use null.

For evaporate, analyze plain_text_observation:
- If the text contains "vacuum" after a mention of "scCO2", "scCO₂", or "supercritical CO2", use true.
- Else use false.
