# MPIF Export

This folder contains a node script to export MOFSY data to the MPIF format.

## Installation

Make sure you have Node.js ≥ 18 and npm (or pnpm/yarn) installed.

Then run on this directory:

```bash
npm install
```

This installs TypeScript, ts-node (for direct execution), and all other dependencies.

## Usage

Run the script with:

```bash
npm start
```